import { motion } from 'framer-motion';
import { Button } from './button';

/**
 * Versão animada do Button do shadcn/ui com suporte a props do Framer Motion
 */
export const MotionButton = motion(Button);
